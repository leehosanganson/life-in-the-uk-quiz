root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000
  kill_delay = "0s"
  send_interrupt = true

[log]
  time = true

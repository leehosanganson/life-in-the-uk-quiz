name: Build Branch Image (On-Demand)

on:
  pull_request:
    types: [labeled]

jobs:
  build-branch:
    if: contains(github.event.pull_request.labels.*.name, 'build-image')
    name: Build Developer Branch
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sanitize Branch Name for Docker Tag
        # Replaces slashes with dashes so 'feature/login' becomes 'feature-login'
        run: |
          CLEAN_BRANCH=$(echo "${{ github.head_ref }}" | tr '/' '-')
          echo "IMAGE_TAG=$CLEAN_BRANCH" >> $GITHUB_ENV

      - name: Login to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Build Backend
        run: |
          cd ./backend
          docker build ./ -t ${{ vars.HARBOR_REGISTRY }}/${{ vars.HARBOR_PROJECT }}/life-in-the-uk-quiz-backend:${{ env.SHORT_SHA }}
          docker push ${{ vars.HARBOR_REGISTRY }}/${{ vars.HARBOR_PROJECT }}/life-in-the-uk-quiz-backend:${{ env.SHORT_SHA }}

      - name: Build Frontend
        run: |
          cd ./frontend
          docker build ./ -t ${{ vars.HARBOR_REGISTRY }}/${{ vars.HARBOR_PROJECT }}/life-in-the-uk-quiz-frontend:${{ env.SHORT_SHA }}
          docker push ${{ vars.HARBOR_REGISTRY }}/${{ vars.HARBOR_PROJECT }}/life-in-the-uk-quiz-frontend:${{ env.SHORT_SHA }}
